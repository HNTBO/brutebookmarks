# Archived new-tab entrypoints

These entrypoints are kept for reference only and are not part of the active WXT build variants.

- `ntp-redirect`: original redirect-only new-tab extension.
- `ntp-native`: original native-only new-tab extension.

The active `newtab` variant uses `../ntp`, which is the former hybrid implementation branded as BruteBookmarks New Tab.
